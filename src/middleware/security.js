const helmet=require("helmet");const applySecurityMiddleware=(app)=>{app.use(helmet({contentSecurityPolicy:false,crossOriginEmbedderPolicy:false}));console.log("Security middleware active");};module.exports={applySecurityMiddleware};